{
  "frontend/src/index.css": "Global stylesheet with Cormorant Garamond and Jost fonts, reverted to the warm neutral palette — cream/off-white backgrounds (OKLCH ~0.97), charcoal foreground (OKLCH ~0.28), warm taupe secondary/muted tones, and terracotta/dusty rose accent (OKLCH ~0.62 hue 28) for both light and dark modes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Cormorant Garamond/Jost fonts, OKLCH color tokens referencing the warm neutral palette CSS variables, and lightened shadow values appropriate for the cream/off-white light theme.",
  "frontend/src/hooks/useQueries.ts": "Central React Query hooks file with all backend operations — products CRUD, cart, orders, store content, user profile, and admin role check. The `useIsCallerAdmin` hook now uses `retry: false` (no retries) and `staleTime: 30_000` (prevents aggressive re-fetching), with a cleaner `isLoading` derivation that only reflects active loading states, fixing the infinite pending issue.",
  "frontend/src/lib/utils.ts": "Utility helpers including cn(), formatPrice() (INR/en-IN locale), and lookup maps for category labels, images, slugs, and the ordered list of all categories. The maleShirts label is now \"Cotton Shirts\" and maleTshirts label is now \"Graphic T-Shirts\".",
  "frontend/src/components/Header.tsx": "Sticky header with category nav, centered logo, cart icon, and the new UserDropdown component replacing the old LoginButton in both desktop and mobile menus; the standalone Admin nav link has been removed in favour of the dropdown.",
  "frontend/src/components/Footer.tsx": "Three-column footer with the Collections column now listing \"Cotton Shirts\" and \"Graphic T-Shirts\" (replacing the old \"Male Shirts\"/\"Male T-Shirts\" labels), and the Studio column now includes an \"Admin Panel\" TanStack Router Link to /admin below \"About Us\", retaining all other footer content and caffeine.ai attribution.",
  "frontend/src/components/Layout.tsx": "Shell layout component using named imports for `Header` and `Footer`, rendering them around an `Outlet`; also mounts `ProfileSetupModal` when an authenticated user has no saved profile, enabling the admin email flow.",
  "frontend/src/components/ProductCard.tsx": "Product card component that now renders the actual stored product image (when it's a valid base64 data URI) with a graceful fallback to the decorative placeholder; includes an `imgError` state to handle broken image URLs without crashing.",
  "frontend/src/pages/Home.tsx": "Homepage that now fetches dynamic store content via useGetStoreContent to display the heroText and heroBanner fields with loading skeletons, falling back to defaults; retains the cover hero image, category grid, and features strip.",
  "frontend/src/pages/Catalog.tsx": "Catalog page with gold accent on active category tab and section labels, updated empty state with gold decorative symbol.",
  "frontend/src/pages/ProductDetail.tsx": "Product detail page that now displays the stored product image (when it's a valid base64 data URI) in the main image panel with a graceful fallback to the decorative placeholder; includes `imgError` state for safe error handling.",
  "frontend/src/pages/Cart.tsx": "Cart page with gold accent on line totals, subtotal display, checkout button, and empty state CTA; quantity controls updated with gold hover states.",
  "frontend/src/pages/Checkout.tsx": "Checkout page with gold accent on total price, place order button, loading spinner, and form focus states; order summary panel with border for definition on dark background.",
  "frontend/src/pages/OrderConfirmation.tsx": "Order confirmation page with gold success icon ring, gold order reference display, gold accent CTA button, and outlined return home link.",
  "frontend/src/App.tsx": "Main app entry with TanStack Router setup using the named `Layout` export; all eight routes registered under the Layout root route.",
  "frontend/index.html": "HTML entry point with page title updated to \"DHIMAYU STUDIO\" in all caps.",
  "frontend/src/pages/Admin.tsx": "Protected admin dashboard with a fixed, clearly-ordered render guard sequence — identity init → auth check → timeout → query error → active loading → not-admin → admin dashboard. The timeout effect no longer resets on every render cycle, and a separate effect clears the timeout flag when the query settles, ensuring the page always transitions out of the loading state.",
  "frontend/src/pages/About.tsx": "About Us page that now fetches dynamic aboutPageCopy from the backend via useGetStoreContent, splitting it into paragraphs by newlines with loading skeletons and a static fallback if no content is set.",
  "frontend/src/components/LoginButton.tsx": "New reusable Login/Logout button component that uses `useInternetIdentity` and `useQueryClient` to handle authentication; shows a spinner during login, a logout icon when authenticated, and a login icon when not authenticated, styled to match the Dhimayu Studio header aesthetic.",
  "frontend/src/components/UserDropdown.tsx": "Dropdown component showing a Login button when unauthenticated, and a user account dropdown when authenticated. Uses `isFetched` from `useIsCallerAdmin` to distinguish between \"still loading\" and \"confirmed result\", showing a spinner only while the check hasn't completed yet and the Admin Panel link only when `isAdmin === true` and `isAdminFetched === true`.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog that appears after first login, prompting the user to enter only their name (no email field); saves the profile via `useSaveCallerUserProfile` and calls `onComplete` on success."
}